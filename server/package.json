{
  "name": "server",
  "version": "1.0.0",
  "description": "Chat App Server",
  "main": "server.js",
  "scripts": {
    "app": "npm run build --prefix ../client && npm start",
    "dev": "npm run start --prefix ../client",
    "premongo": "mkdir mongodb",
    "mongodb": "start mongod --dbpath mongodb --nojournal"
  },
  "author": "Mr KSM",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "mongodb": "^3.1.6",
    "socket.io": "^2.1.1"
  }
}
